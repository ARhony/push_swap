#ifndef TOOLS_H
# define TOOLS_H

# include "../libft/libft.h"

void    rotate(t_list **p);
void    swap(t_list **p);
void    push(t_list **f, t_list **t);
void    reverse_rotate(t_list **p);

#endif
